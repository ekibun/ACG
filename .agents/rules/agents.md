---
trigger: always_on
---

遵守仓库根目录的 [AGENTS.md](../../AGENTS.md) —— 它是本项目的刚性约定，本文件不重复其内容。

本文件只为**按 `.agents/rules/` 约定加载规则的宿主**（Cursor 系、部分 CLI agent）而存在，
是一个指向根 `AGENTS.md` 的指针。**不要在这里写规则**：规则放根 `AGENTS.md`，
可查阅的参考资料放 [`../skills/`](../skills)。
